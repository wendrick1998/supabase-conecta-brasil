
import { LucideIcon, Video as VideoIcon } from 'lucide-react';

// This is a wrapper for the Video icon to avoid naming conflicts
export const Video: LucideIcon = VideoIcon;

// We can add other icon re-exports here as needed
